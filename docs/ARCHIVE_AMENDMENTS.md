# ARCHIVE AMENDMENTS — when notation finds a "finished" object wrong

> Created day 22 (2026-08-21) at the composer's request, on the first case:
> *"let's also have some sort of documentation protocol when we're dealing
> with legacy archive objects that are, quote, unquote, finished."*
> Protocol first, ledger below. One line per amendment; never delete lines —
> change their STATUS.

## The protocol

1. **The archive is frozen.** The canonical piece score
   (`scores/piece-s25-finished01.json`) and its objects are never edited by
   the AI as a side effect of notation work. "Finished" means finished.
2. **Corrections live in the IR — the notation's truth.** When tier-2 work
   shows an archive object to be wrong or under-specified for sound or page,
   the corrected value goes into the version file (`notation/ir/*.ir.json`):
   - **systematic** → an extractor rule (e.g. the 2n law: fixed one-shots'
     `duration` = their measured sample length), so every sibling inherits it;
   - **singular** → a per-item override in the IR's overlays channel.
3. **The notation app's playback follows the IR**, not the archive
   (`midiplayer.withIrDurations`: a per-play clone of the score whose object
   ends follow the IR's event durations — the archive object is untouched).
   What you hear in the loop is the amended note.
4. **Every amendment gets a ledger line here** the moment it is made: object ·
   what the archive says · what the IR says · why · the evidence · status.
5. **A COMPOSITIONAL edit is not an amendment, and it goes in the SCORE**
   *(added day 24, first case: moving `wc-28` from T2 to T9 on the
   composer's instruction).* Rules 1-4 govern an archive value being WRONG.
   When the composer instead changes the music — who plays a note, when, on
   what pitch — the IR cannot carry it: a part move changes which MIDI port
   sounds the note, and the player compiles the ARCHIVE (with IR durations
   applied), so an IR-only move would draw the note on the new staff while
   it kept sounding on the old port. That divergence is the thing this
   document exists to prevent. So: the edit goes in the score, it is made by
   an explicit script (`tools/move_object.js`) rather than by hand, it gets a
   ledger line marked **SCORE EDIT**, and every IR built from that score is
   re-extracted in the same breath. `git checkout -- scores/<name>.json` is
   the undo.
6. **Folding back into the archive is a separate, explicit composer act**
   (a script run from this ledger), never automatic. Until then the
   composer app and any render made from the raw archive still play the
   archive's values — a known divergence, stated per line below.

## Known gaps (so the divergence is never a surprise)

- `tools/export_midi.js` compiles the RAW archive — a Reaper render made
  from it plays the un-amended notes (wc-23's fp cut at 0.70 s). Fix when a
  render is next made: `--ir <id>` applying `withIrDurations` (NITS).
- The composer app (`composer.html`) plays the archive as drawn.

## Ledger

| Date | Object | Archive says | IR says | Why | Evidence | Status |
|---|---|---|---|---|---|---|
| 2026-08-21 | `wc-23` (T1, G#1 fortepiano, `grp-g1-opening`) | drawn 14.544–15.243 (0.70 s); note-off at 15.243 | duration 1.49 s → sounds to 16.034 (2n: measured fp sample length at MIDI 32) | the note-off cut the sample roughly in half; composer heard it ("cuts out about halfway through the line") and chose the full sample ("that one was the whole bar more or less, let's use that one") | probe `db1-t1-x03` (archive copy with only wc-23 lengthened): headless compile off@15.243 vs off@16.034, same on/port/ch; composer's ear verdict on x03 | IR-only (systematic: every fp/staccato/cuivre inherits via the 2n rule in extraction) |
| 2026-08-22 | `wc-29` (T1, G1 staccato, `grp-g1-opening`) | drawn 17.749–18.035 (0.286 s); note-off at 18.035 | duration 0.46 s → sounds to 18.209 (2n: measured staccato sample length at MIDI 31) | same law as wc-23 (D49/D51): the drawn object is 0.17 s short of the sample; the tag `STAC-rev` = "reverted to staccato at original duration" (commit 2bd18e1) — provenance, not a length decision | extraction applied the 2n rule; `test_midiplayer` now asserts the amended list = every IR event whose duration differs from the drawn object | IR-only (systematic) |
| 2026-08-22 | `wc-28` (staccato G1/midi 31, 17.19 s, `grp-g1-opening`, `STAC-rev`) | part **T2** (`tuba2b` ch 3) | part **T9** (`tuba9b` ch 3) | composer instruction — a COMPOSITIONAL move, not a correction: *"in tuba two at seventeen nineteen there is a GC. Can we move that to another part, two by nine?"* T9 was the only part free there (empty until 21.07, the sparsest in the section, no surge) | `tools/move_object.js` dry run + one-line diff (`"layer": 1` → `8`); live page shows the 17.19 GC on T9's lane and none in T2 before 22.66; `techniqueFor` routes it to `tuba9b` where it was `tuba2b`; IR re-extracted and revalidated, 8 batteries green | **SCORE EDIT (archive)** — applied, not IR-only |
| 2026-08-22 | `wc-87` (fortepiano B2/midi 47, 30.66 s, `grp-g1-opening`, `FP3x`) | part **T8** (`tuba8`) | part **T9** (`tuba9`) | composer instruction — a COMPOSITIONAL move made FOR the ring bar: in T8 the next attack was 0.512 s away, so the breath rule left the bar 0.012 s and it effectively vanished. *"Let's move that whole figure to t nine and give it its full length and bar back."* | bar 0.012 s → **0.975 s** measured after the move (T9's next attack is 1.475 s away, less the 0.5 s breath); `techniqueFor` routes it to `tuba9` where it was `tuba8`; one-line diff; IR re-extracted and revalidated | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-88` (fortepiano F#1/midi 30, 30.73 s, `grp-g1-opening`, `FP3x`) | part **T10** (`tuba10`) | part **T7** (`tuba7`) | composer instruction — a COMPOSITIONAL move. T10 was the one part whose density stretch resisted every metric reading (nine staccatos, no gap over a breath, nothing under 16 ms); moving the fp out leaves T10 all-staccato and gives the fp more room in T7 | ring bar 0.309 s → **0.426 s** measured after the move (T7 next attack 31.661 vs T10 31.544, less the 0.5 s breath); `techniqueFor` routes it to `tuba7` where it was `tuba10`; one-line diff; IR re-extracted, 128 chunks, valid | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1890` (staccato midi 32 at 39.80 s, `grp-cloud02-i-01`) | part **T1 (tuba1)** | part **T10 (tuba10)** | THE PLAYABILITY PROCESS (day 25): re-attack 130 ms vs 157 needed (G#2→D#2, 5 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1910` (staccato midi 39 at 40.30 s, `grp-cloud02-i-01`) | part **T1 (tuba1)** | part **T8 (tuba8)** | THE PLAYABILITY PROCESS (day 25): the same T1 tail pair, second flag. Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1894` (staccato midi 44 at 39.85 s, `grp-cloud02-i-01`) | part **T3 (tuba3)** | part **T9 (tuba9)** | THE PLAYABILITY PROCESS (day 25): re-attack 150 ms vs 175 needed (C#2→G#2, 7 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1903` (staccato midi 37 at 40.17 s, `grp-cloud02-i-01`) | part **T3 (tuba3)** | part **T5 (tuba5)** | THE PLAYABILITY PROCESS (day 25): FIRST-note pass: T3 @40.31 had no home for the second note; moving the first cleared it. Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1898` (staccato midi 37 at 40.00 s, `grp-cloud02-i-01`) | part **T4 (tuba4)** | part **T3 (tuba3)** | THE PLAYABILITY PROCESS (day 25): re-attack 146 ms vs 147 needed (F2→C#2, 4 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1781` (staccato midi 31 at 36.86 s, `grp-cloud02-i-01`) | part **T5 (tuba5)** | part **T1 (tuba1)** | THE PLAYABILITY PROCESS (day 25): re-attack 161 ms vs 166 needed (C#2→G1, 6 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1768` (staccato midi 34 at 36.46 s, `grp-cloud02-i-01`) | part **T6 (tuba6)** | part **T9 (tuba9)** | THE PLAYABILITY PROCESS (day 25): re-attack 136 ms vs 138 needed (G1→A#1, 3 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1914` (staccato midi 35 at 40.33 s, `grp-cloud02-i-01`) | part **T6 (tuba6)** | part **T4 (tuba4)** | THE PLAYABILITY PROCESS (day 25): re-attack 147 ms vs 147 needed (D#2→B1, 4 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1816` (staccato midi 31 at 37.84 s, `grp-cloud02-i-01`) | part **T7 (tuba7)** | part **T9 (tuba9)** | THE PLAYABILITY PROCESS (day 25): re-attack 154 ms vs 194 needed (E2→G1, 9 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1869` (staccato midi 37 at 39.21 s, `grp-cloud02-i-01`) | part **T7 (tuba7)** | part **T9 (tuba9)** | THE PLAYABILITY PROCESS (day 25): re-attack 133 ms vs 138 needed (E2→C#2, 3 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1878` (staccato midi 36 at 39.51 s, `grp-cloud02-i-01`) | part **T9 (tuba9)** | part **T3 (tuba3)** | THE PLAYABILITY PROCESS (day 25): re-attack 146 ms vs 157 needed (F2→C2, 5 st). Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `wc-1905` (staccato midi 34 at 40.18 s, `grp-cloud02-i-01`) | part **T9 (tuba9)** | part **T3 (tuba3)** | THE PLAYABILITY PROCESS (day 25): re-attack 152 ms vs 166 needed (E2→A#1, 6 st); enabled by wc-1903 leaving T3 first. Redistribution only — no note removed, no time or pitch changed | `tools/move_object.js --apply`, one-field diff; `audit_playability` on the amended window: **0 hard, 0 soft** (was 13 soft); the pre-amendment fixture is `scores/cloud02i-orig.json` | **SCORE EDIT (archive)** — applied |
| 2026-08-23 | `grp-cloud02-i-01` (all 159 staccato notes) | written lengths **50–218 ms** — the played lengths, carried from `clusterClouds02` through `cloud02.js` | all **50 ms** | composer instruction: *"these should all be staccatos… some of the bricks are longer. That must have been from my playing."* Also a playability fact: an over-long brick reads as a HARD conflict when the attack gap is comfortable — the 12 moves above created two such artefacts (T4 @40.33 from a 202 ms brick over a 174 ms gap; T9 @36.46 from 182 ms over 133 ms) | `tools/set_brick.js --apply`; whole-score hard **4 → 2** (the 2 left are the trance seams @560.63 / @604.63, out of scope); soft unchanged; SOUND unaffected — D51: a fixed one-shot lasts its sample length and the IR carries it | **SCORE EDIT (archive)** — applied, systematic |
| 2026-08-24 | `wc-1931` (staccato midi 50 at 43.63 s) | part T6 (tuba6) | part T9 (tuba9) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 169 ms vs 231 needed, pair with wc-1929 (second-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1956` (staccato midi 61 at 44.69 s) | part T2 (tuba2) | part T4 (tuba4) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 225 ms vs 268 needed, pair with wc-1948 (second-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1958` (staccato midi 62 at 44.73 s) | part T7 (tuba7) | part T8 (tuba8) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 182 ms vs 287 needed, pair with wc-1951 (second-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1967` (staccato midi 63 at 44.95 s) | part T5 (tuba5) | part T7 (tuba7) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 139 ms vs 194 needed, pair with wc-1960 (second-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1961` (staccato midi 52 at 44.83 s) | part T10 (tuba10) | part T1 (tuba1) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 175 ms vs 231 needed, pair with wc-1970 (first-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1976` (staccato midi 56 at 45.15 s) | part T4 (tuba4) | part T8 (tuba8) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 131 ms vs 147 needed, pair with wc-1984 (first-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1999` (staccato midi 43 at 45.66 s) | part T10 (tuba10) | part T8 (tuba8) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 149 ms vs 231 needed, pair with wc-2008 (first-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-2011` (staccato midi 61 at 45.95 s) | part T10 (tuba10) | part T5 (tuba5) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): soft re-attack 140 ms vs 157 needed, pair with wc-2008 (second-note pass) | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1991` (staccato midi 39 at 45.47 s) | part T7 (tuba7) | part T1 (tuba1) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes D#2 after wc-1975, 329 ms vs 277 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1994` (staccato midi 41 at 45.51 s) | part T5 (tuba5) | part T2 (tuba2) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes F2 after wc-1981, 243 ms vs 305 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1988` (staccato midi 48 at 45.45 s) | part T8 (tuba8) | part T3 (tuba3) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes C3 after wc-1982, 172 ms vs 166 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1995` (staccato midi 40 at 45.51 s) | part T6 (tuba6) | part T5 (tuba5) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes E2 after wc-1985, 200 ms vs 250 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1997` (staccato midi 54 at 45.53 s) | part T9 (tuba9) | part T6 (tuba6) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes F#3 after wc-1987, 149 ms vs 184 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1993` (staccato midi 45 at 45.49 s) | part T2 (tuba2) | part T7 (tuba7) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes A2 after wc-1986, 156 ms vs 194 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1990` (staccato midi 47 at 45.45 s) | part T1 (tuba1) | part T8 (tuba8) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes B2 after wc-1980, 183 ms vs 194 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `wc-1996` (staccato midi 51 at 45.52 s) | part T3 (tuba3) | part T9 (tuba9) | THE PLAYABILITY PROCESS (`tools/playability.js --section CLOUD02-D`): joint re-seating of the 45.45 s collapse (10 parts, worst leap 57% → 20% short) — this part now takes D#3 after wc-1983, 246 ms vs 231 needed | window re-audited from disk after applying: 0 hard, 10 soft | SCORE EDIT (archive) — applied |
| 2026-08-24 | `grp-cloud02-d-01` (110 staccato notes) | written lengths 50–221 ms (played) | all 50 ms | composer instruction: "these should all be staccatos… some of the bricks are longer, that must have been from my playing" — and an over-long brick reads as a HARD playability conflict when the attack gap is comfortable | hard 2 -> 2 on the whole score; sound unchanged (D51, IR carries duration) | SCORE EDIT (archive) — applied |

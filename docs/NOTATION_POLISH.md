# NOTATION POLISH — the tier-3 ledger

> **Working rule (D18 applied to notation; preplan §3):** during tier-2
> part-by-part work, micro-layout defects are FILED HERE, never surfaced as
> decisions and never discussed mid-notation — unless one actually blocks
> reading the material. The tier-3 pass (POL) works this ledger
> fine-tooth-comb WITH the composer at the very end: every item fixed via
> the data channels (engraving registry · engraving-override overlays ·
> per-page lane weights · the fix ladder in container.json
> verticalBudget._fixLadder) or explicitly waived.
>
> Entries come from two sources: the composer's or AI's eye (filed by
> hand, one line each), and `tools/protrusion_detect.js` (geometric
> auto-detection; appends below its marker). Each entry: WHERE it is
> visible (ir/score · window · part · time) + WHAT is wrong. No fixes, no
> discussion here.

## Hand-filed

- `db1` · T9 @ 36.87 s — the 6:4 bracket line crosses its own group's accent glyph (76 ms x-overlap, 0.2 ss) — pre-existing in the day-30 approved material, surfaced day 31 by the new notate_section geometry guard; fix via the stack (tier 3)

## Auto-detected (tools/protrusion_detect.js appends below; do not edit by hand)*

### 2026-08-21 — piece-open-01, piece-trance-w1, trance-section-01

- `piece-open-01` · T1 @ 30.00 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-open-01` · T1 @ 32.58 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T1 @ 34.51 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T2 @ 29.65 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-open-01` · T2 @ 36.76 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T5 @ 34.34 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T5 @ 39.61 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T6 @ 24.28 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T7 @ 14.34 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-open-01` · T7 @ 37.02 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T8 @ 32.44 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T8 @ 36.55 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-open-01` · T10 @ 30.73 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-open-01` · T10 @ 36.31 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-trance-w1` · T3 @ 525.43 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-trance-w1` · T6 @ 519.43 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-trance-w1` · T6 @ 522.63 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `piece-trance-w1` · T10 @ 517.03 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `piece-trance-w1` · T10 @ 527.43 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `trance-section-01` · T3 @ 25.60 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `trance-section-01` · T6 @ 19.60 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `trance-section-01` · T6 @ 22.80 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `trance-section-01` · T10 @ 17.20 s — dot crosses the bottom lane edge into the neighbor by ~5.4 px
- `trance-section-01` · T10 @ 27.60 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px

### 2026-08-22 — db1-all-x01

- `db1-all-x01` · T1 @ 4.20 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T1 @ 30.00 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T1 @ 32.58 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T1 @ 34.51 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T2 @ 29.65 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T2 @ 36.76 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T5 @ 2.00 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T5 @ 34.34 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T5 @ 39.61 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T6 @ 24.28 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T7 @ 14.34 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T7 @ 34.55 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T7 @ 37.02 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T8 @ 32.44 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T8 @ 36.55 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T10 @ 30.73 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
- `db1-all-x01` · T10 @ 36.31 s — glyph:accidental-sharp crosses the bottom lane edge into the neighbor by ~3.1 px
